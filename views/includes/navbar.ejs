<nav class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom border-secondary shadow-sm sticky-top">
  <div class="container">
    
    <!-- Centered Logo on Mobile -->
    <a class="navbar-brand mx-auto d-lg-none" href="/listing">
      <span class="h3 fw-bold text-primary">Tripo</span><span class="h3 fw-bold text-warning">Go</span>
    </a>

    <!-- Toggle button with animation -->
    <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar"
      aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Left Logo (for desktop view) -->
    <a class="navbar-brand d-none d-lg-block me-4" href="/listing">
      <span class="h3 fw-bold text-primary">Tripo</span><span class="h3 fw-bold text-warning">Go</span>
    </a>

    <!-- Navbar Content -->
    <div class="collapse navbar-collapse" id="mainNavbar">
      <!-- Close button inside mobile menu -->
      <div class="d-lg-none text-end w-100">
        <button class="btn btn-close btn-close-white me-2 mt-2" type="button" data-bs-toggle="collapse"
          data-bs-target="#mainNavbar" aria-label="Close"></button>
      </div>

      <!-- Center nav links -->
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0 align-items-center">
        
        <li class="nav-item mx-2">
          <a class="nav-link px-3 position-relative" href="/listing">
            <i class="bi bi-list-ul me-1"></i> Listings
          </a>
        </li>
      </ul>

      <!-- Right side links -->
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
        <% if (!currentUser) { %>
        <li class="nav-item mx-1">
          <a class="btn btn-outline-light px-3 position-relative" href="/signup">
            <i class="bi bi-person-plus me-1"></i> Sign Up
          </a>
        </li>
        <% } %>

        <% if (currentUser) { %>
        <li class="nav-item ms-2">
          <a class="btn btn-outline-light px-3 position-relative" href="/logout">
            <i class="bi bi-person-plus me-1"></i> Logout
          </a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<!-- Bootstrap Icons CSS -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">

<!-- Custom Styles -->
<style>
  /* Navbar hover effects */
  .nav-link {
    color: rgba(255, 255, 255, 0.8) !important;
    transition: all 0.3s ease;
    font-weight: 500;
  }

  .nav-link::after {
    content: '';
    position: absolute;
    width: 0;
    height: 2px;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    background-color: #0d6efd;
    transition: width 0.3s ease;
  }

  .nav-link:hover {
    color: white !important;
  }

  .nav-link:hover::after {
    width: 60%;
  }

  /* Button hover effects */
  .btn-outline-light {
    transition: all 0.3s ease;
  }

  .btn-outline-light:hover {
    background-color: rgba(255, 255, 255, 0.1);
    transform: translateY(-2px);
  }

  .btn-outline-danger {
    transition: all 0.3s ease;
  }

  .btn-outline-danger:hover {
    transform: translateY(-2px);
  }

  /* Logo hover effect */
  .navbar-brand:hover span {
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.3);
  }

  /* Mobile menu styles */
  @media (max-width: 992px) {
    .navbar-collapse {
      background-color: #212529;
      padding: 1rem;
      margin-top: 0.5rem;
      border-radius: 0.25rem;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
    }

    .nav-link::after {
      display: none;
    }

    .nav-item {
      margin-bottom: 0.5rem;
    }

    .btn-close {
      font-size: 1.5rem;
      opacity: 1;
      transition: transform 0.3s ease;
    }

    .btn-close:hover {
      transform: rotate(90deg);
    }
  }

  /* Toggle button animation */
  .navbar-toggler {
    transition: transform 0.3s ease;
  }

  .navbar-toggler:not(.collapsed) {
    transform: rotate(90deg);
  }
</style>
